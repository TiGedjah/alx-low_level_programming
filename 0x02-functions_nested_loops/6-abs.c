#include <stdio.h>
#include "main.h"

/**
 * _abs - return the absolute value of an integer
 * @a: the check reference
 * Return: Return
 */

int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
