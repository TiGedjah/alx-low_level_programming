#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - prints out the length of the string
 * @s: the string whose to print out
 * Return: 0 on success
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{
		a = a;
	}

	return (a);
}
